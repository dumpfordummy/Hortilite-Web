This is a web ui for Hortilite system to fetch and display data

Steps to install after clone
1. run `npm install` to install dependencies listed in 'package-lock.json'

To run the project
1. run `npm run dev`
